name: tool_text
description: "Smart Text Tools - A collection of powerful text processing utilities."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  share_plus: ^10.1.4
  google_fonts: ^6.2.1
  google_mlkit_text_recognition: ^0.14.0
  image_picker: ^1.1.2
  provider: ^6.1.2
  shared_preferences: ^2.3.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true # Enable for localizations
